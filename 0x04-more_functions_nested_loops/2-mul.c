#include "main.h"
/**
 * mul - Multiple two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with first number
 * Return: The result of multiplication between x para, and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
